# SingleTon 코드

    멀티 쓰레드 환경에서 하나의 매니저만 생성, 작동하도록 해 충돌을 방지하는 패턴

코드 형식은 다양할 수 있지만 한번 쓴 걸 우려먹는 게 좋아 보인다.
이번 수업 중에 예시로 작성된 코드, 타입을 유동적으로 받을 수 있으므로 편해보인다.
## Singleton 예시
```C#
using UnityEngine;

public class Singleton<T> : MonoBehaviour where T : MonoBehaviour
{
    private static object _lockObject = new();
    private static T _instance = null;

    public static T Instance
    {
        get
        {
            lock(_lockObject)
            {
                if(_instance != null)
                {
                    return _instance;
                }

                var obj = new GameObject(typeof(T).Name);
                _instance = obj.AddComponent<T>();
                DontDestroyOnLoad(_instance);
                return _instance;
            }
        }
    }
}
```
## 사운드 매니저 예시
```C#
[RequireComponent(typeof(AudioSource))]
public class SoundManager : Singleton<SoundManager>
``` 
*첫 번째 줄의 경우 오디오 소스가 반드시 필요하며 없을 시 생성한다는 의미*

* * *

### 다시만들기 버그 수정
##### 빌드 후에 이동속도가 현저히 느려지는 문제가 생겼다!
해상도 문제인 줄 알고 한참 뻘짓을 함

#### 게임뷰 프레임 제한이 없고 유니티 기본 빌드 설정은 60프레임 제한이였던 탓!
    Time.DeltaTime 곱하는 걸 항상 잊지 말자.... 

